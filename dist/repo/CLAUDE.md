@AGENTS.md

# Claude Code adapter note

Claude Code project skills are generated under
`dist/repo/.claude/skills/`. Edit canonical packages and Claude overlays, then
use `scripts/sync-adapters`; never edit the generated copies directly.
