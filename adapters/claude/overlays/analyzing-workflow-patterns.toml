schema_version = "1"
provider = "claude"
skill = "analyzing-workflow-patterns"
granted_actions = ["filesystem.read", "network.read"]

[frontmatter]
allowed-tools = "Read Grep Glob WebFetch WebSearch"

